name: Merge Validation

on:
  pull_request:
    branches:
      - dev

jobs:
  merge_validation:
    runs-on: ubuntu-latest
    steps:
      - name: Check base branch
        if: github.event.pull_request.base.ref != 'main'
        run: |
          echo "Only merge from dev to main"
          exit 1
